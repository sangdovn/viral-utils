import type { DouyinUserStatus } from "@/pages/Douyin/types";

export const NO_SYSTEM_VALUE = "__none__";
export const DEFAULT_STATUSES: DouyinUserStatus[] = ["active", "testing", "pending", "dropped"];
export const VIDEO_PAGE_SIZE = 50;
